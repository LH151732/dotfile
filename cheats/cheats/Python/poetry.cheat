% poetry,

# get coverage
poetry add -dev coverage

# Run test report
poetry run coverage report

# Run test report in html
poetry run coverage html

# Run test report in xml
poetry run coverage xml

# Run pytest
poetry run coverage run -m pytest
